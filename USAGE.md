# 农户补贴管理系统 — 使用手册

## 一、快速开始

### 1. 获取系统
将整个文件夹复制到你的电脑上（任意位置，如桌面或 D 盘）。

文件夹内容：
```
SubsidySystem（发放文件夹）/
├── SubsidySystem.exe    ← 主程序，双击启动
├── subsidy.db            ← 数据库文件（自动生成，存储所有数据）
└── 使用说明.txt           ← 本说明
```

### 2. 启动系统
**双击 `SubsidySystem.exe`**，会出现一个黑色命令行窗口（不要关闭它），稍等 1-2 秒浏览器会自动打开系统页面。

> 如果浏览器没有自动打开，手动在浏览器地址栏输入：**http://127.0.0.1:8000**

### 3. 停止系统
在黑色命令行窗口中按 **Ctrl + C**，或直接关闭窗口。

---

## 二、功能概览

| 功能模块 | 说明 |
|---------|------|
| 首页看板 | 补贴概览、统计数据、快捷入口 |
| 农户管理 | 管理所有农户信息（家庭户、个人档案） |
| 补贴项目 | 创建补贴项目，管理预申请/发放/代领 |
| 项目进度 | 跟踪各村补贴工作进度（阶段卡片） |
| 村组管理 | 管理村组结构、土地信息、联系人 |
| 土地流转 | 代耕代种土地登记 |
| 大户管理 | 大户/新型经营主体管理 |
| 数据导入 | Excel 批量导入农户、补贴数据 |
| 数据导出 | Excel 导出、备份迁移 |

---

## 三、常见操作

### 新建补贴项目
1. 点击「补贴项目」→「新增项目」
2. 填写项目名称、年度、补贴标准
3. 在项目详情页进行数据导入或手动添加

### 导入农户/补贴数据
1. 进入相应管理页面
2. 点击「导入 Excel」按钮
3. 选择 Excel 文件（支持 .xlsx / .xls）
4. 选择对应的 Sheet
5. 系统会自动匹配列名，确认后开始导入
6. 导入完成后如有失败数据，可点击「导出失败数据」下载排查

### 查看项目进度
1. 点击「项目进度」
2. 选择补贴项目
3. 各村以卡片形式展示，颜色表示进度状态
4. 点击卡片展开查看/编辑各阶段状态

### 数据备份
1. 进入「系统设置」→「备份迁移」
2. 点击「下载备份」保存 `.db` 文件
3. 恢复时将 `.db` 文件放回程序目录即可

---

## 四、多人共享使用（局域网）

如果办公室多人需要使用，可以部署在一台电脑上：

1. 找一台常开的电脑（如办公室公用电脑）
2. 将文件夹复制到该电脑，双击启动 `SubsidySystem.exe`
3. 查看该电脑的 IP 地址（Win+R 输入 `cmd`，再输入 `ipconfig`，找到 `192.168.x.x`）
4. 其他人电脑浏览器输入 `http://[该电脑IP]:8000` 即可访问

示例：主电脑 IP 为 `192.168.1.100`，其他人访问 `http://192.168.1.100:8000`

---

## 五、注意事项

1. **不要删除 `subsidy.db` 文件**，这是数据库，存了所有数据
2. 首次使用建议先初始化村组数据（在「村组管理」中操作）
3. 定期备份数据（系统设置 → 备份迁移 → 下载备份）
4. 导入 Excel 前可以先下载模板，按模板格式填写能避免导入错误
5. 系统关闭前确保数据已保存（一般操作即自动保存）

---

## 六、常见问题

**Q: 浏览器提示"无法访问"？**
A: 确认 `SubsidySystem.exe` 正在运行（黑色窗口没有关闭）。

**Q: 双击 EXE 没反应？**
A: 右键 → 以管理员身份运行。或检查防火墙是否拦截。

**Q: 数据在哪里？**
A: 所有数据存储在 EXE 同目录下的 `subsidy.db` 文件中。

**Q: 怎么换电脑？**
A: 复制整个文件夹到新电脑，`subsidy.db` 文件包含了所有数据。

**Q: 可以导入哪些 Excel 格式？**
A: 支持 `.xlsx` 和 `.xls` 格式。建议使用各页面提供的模板。
