# 农户补贴管理系统 - 最终 UI 开发规范 (AI 综合版)

> **版本说明**：本规范融合了“新中式极简主义”风格与用户提供的详细开发参数。请严格遵守文中的 **Hex 色值**、**字号** 与 **JS 交互逻辑**。

---

## 1. 项目风格概述
- **风格定位**：稳健、清晰、高效的数字化后台。融合传统东方色彩与现代栅格系统。
- **布局模式**：**固定三栏结构**（顶部通栏 + 左侧侧边栏 + 右侧主内容区）。

---

## 2. 色彩系统 (强制规范)
所有颜色必须以 HEX 代码为准，禁止使用模糊的颜色名称。

| 用途分类 | 色值 | 应用场景 |
| :--- | :--- | :--- |
| **主色调** | `#1A4D3A` (深青绿) | 导航选中态、主要按钮、标题强调、选中项标识线 |
| **辅助色** | `#2C6B52` (浅青绿) | 数据进度条、次要按钮、弱化强调 |
| **页面背景** | `#F8F5F0` (米杏色) | 整体页面背景、大面积底纹 |
| **卡片底色** | `#FFFFFF` (纯白) | 所有内容卡片、弹窗、表单背景 |
| **边框分割** | `#E8E2D9` (浅米色) | 卡片边框、分割线、列表下划线 |
| **成功状态** | `#4CAF50` (绿色) | 成功提示、已通过标签 |
| **危险/删除** | `#D32F2F` (暗红) | 删除操作、警告状态 |
| **编辑操作** | `#E6C288` (浅卡其) | 编辑按钮、次要操作标识 |

---

## 3. 字体规范 (强制字号)
- **全局字体**：`微软雅黑` / `PingFang SC` (无衬线)。
- **系统大标题**：`20px`，加粗。
- **导航文字**：`15px`，常规 / 加粗。
- **模块卡片标题**：`15px`，加粗。
- **正文列表文字**：`14px`。
- **备注/辅助文字**：`12px`，颜色建议 `#999999`。
- **核心数据 (面积/金额)**：`18px ~ 24px`，加粗突出。

---

## 4. 布局结构规范 (固定三栏)
页面必须严格遵循以下三个区域的逻辑划分：

### A. 顶部通栏导航 (Header)
- **左侧**：系统 Logo + 名称。
- **中间**：全局导航菜单 (首页、户籍管理等)。
- **右侧**：用户信息 (头像 + 名称 + 系统设置)。

### B. 左侧功能侧边栏 (Sidebar) - 宽度建议 `320px` - `360px`
- **上层**：筛选区（搜索框、下拉选择器）。
- **中层**：功能按钮区（创建新家庭户、刷新缓存等）。
- **底层**：数据列表区（家庭户卡片列表，支持滚动）。

### C. 右侧主内容区 (Main Content) - 自适应填充
- **顶部**：数据概览卡片 (汇总历史 + 核心承包面积卡片)。
- **中部**：分类数据卡片 (补贴面积使用情况表格)。
- **底部**：成员列表/表单详情 (人员信息及操作)。

---

## 5. 组件统一规范 (UI 细节)

### 5.1 按钮规范
- **主操作按钮**：背景 `#1A4D3A`，文字白色，圆角 `4px`，高度 `32px`。
- **次要功能按钮**：背景透明，边框 `1px solid #E8E2D9`。
- **危险按钮**：背景 `#D32F2F`，白色文字。
- **编辑按钮**：背景 `#E6C288`，白色文字。
- **交互反馈**：所有按钮 `hover` 时 `opacity: 0.9`，禁止使用渐变。

### 5.2 卡片与列表规范
- **卡片**：圆角 `8px`，内边距 `16px~20px`，统一浅灰色边框 (`#E8E2D9`)，间距 `16px`。
- **列表项**：上下内边距 `12px`，`hover` 时底色高亮 (例如 `rgba(26, 77, 58, 0.05)`)。
- **选中态**：选中列表项时，左侧必须添加 `4px` 宽的 `#1A4D3A` 标识线。

### 5.3 筛选组件
- 搜索框统一高度 `32px`，内部自带搜索图标。
- 下拉选择框样式统一（无边框或浅色边框）。
- 多选框横向排布，间距一致。

---

## 6. JS 交互统一规范 (必须实现)
所有前端数据交互必须基于 **JS 数组**，以便未来无缝对接后端接口。

1.  **列表点击渲染**：
    - 点击左侧侧边栏的任何一个“家庭户”项，右侧内容区必须 **立即重新渲染** 该户的所有信息 (面积、成员、补贴详情)。
    - 必须**高亮**当前选中的列表项。

2.  **搜索功能**：
    - 搜索框输入时进行**实时模糊匹配**。
    - 匹配范围：户主姓名、家庭户名称。匹配结果实时更新左侧列表。

3.  **按钮交互**：
    - **新增 / 编辑**：点击后弹窗（Modal）或跳转到编辑页面。
    - **确认 / 刷新**：操作成功后弹出轻提示（Toast），2秒后自动消失。
    - **删除**：必须触发**强制二次确认弹窗**，用户确认后方可执行删除逻辑。

4.  **数据渲染**：
    - 所有文本数据（姓名、面积、人口、补贴额度）必须从 JS 变量数组中读取。
    - 数据更新后，视图需自动同步刷新。

5.  **状态切换**：
    - 标签页 (Tab) 切换时，当前选中项高亮。
    - 勾选框 (Checkbox) 选中/取消选中样式全局统一。

---

## 7. 后续开发与 AI 协作建议
- **优先使用 CSS Grid 进行主体布局，Flexbox 处理列表对齐。**
- **严格按照上述 HEX 色值编写 CSS 变量（`:root { --primary: #1A4D3A; ... }`），便于后期维护。**
- **将上述“JS 交互规范”直接复制给 AI 编程助手，可大幅减少调试时间。**